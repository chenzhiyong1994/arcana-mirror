# v1.1 完整 78 张牌组验收记录

## 自动化结果

2026-07-25 已执行：

```powershell
npm run validate:assets
npm run typecheck
npm test
```

结果：

- `validate:assets` 通过：56 张小阿尔卡那正式源图为 1024×1536；
- `deck` 分包包含 78 张牌面与 1 张卡背，均为 384×576；2026-07-27 视觉接入后分包共 1,689,253 bytes；
- 主包包含 78 张 192×288 缩略图和共享成牌组件；2026-07-27 排除 `deck` 分包后共 578,616 bytes；
- TypeScript 类型检查通过；
- Vitest 5 个测试文件、37 个测试通过；
- 覆盖 78 张连续序号与唯一 ID、22+56 数量、四花色各 14 张完整牌阶、单牌全牌可达、三牌不重复、图片与缩略图路径唯一、受控牌义和原有安全/降级契约。

## 视觉资产验收

- 权杖、圣杯、宝剑、星币四张 7×2 联排已复验；
- 56 张正式源图全部使用 A“仪式典藏”，无标题、编号、Logo、水印和外边框；
- 数字牌花色物件逐张计数，宫廷牌身份、持物和动势逐张检查；
- 圣杯九、宝剑十、星币六、星币八的返工原因和最终结论记录于 `assets/tarot-card-style/minor-arcana/generation-report.md`；
- 星币八最后一次返工移除残留外框并保持七枚墙面星币加一枚手中星币，合计八枚。

## 路由与静态检查

- `app.json` 只在主包注册首页、提问和设置；
- 普通分包 `packages/deck` 注册仪式、结果、历史和图鉴；
- 所有主流程导航均已改为 `/packages/deck/pages/...`；
- 全部小程序 JSON 文件可解析；
- 旧主包页面目录无文件，旧主包 78 张展示图副本已删除；
- 主包首页使用 `getCardThumbnailPath`，分包页面使用 `getCardImagePath`；
- `project.config.json` 没有忽略分包资源。
- `tarot-card-face` 已在首页、仪式、结果、历史、图鉴和鉴赏大图六个消费者中注册；资产校验会阻止任一入口退回裸图。

## 统一成牌组件复验

2026-07-27 在本机微信开发者工具中重新编译并复验：

- 首页今日牌与最近记录中的小阿尔卡那已显示统一旧金框与身份铭牌；
- 小阿尔卡那结果页显示顶部牌阶、双层边框、四角符印和底部中英文铭牌；
- 小阿尔卡那鉴赏大图无裁切或文字溢出，逆位按实体牌效果旋转完整成牌；
- 大阿尔卡那结果页沿用原图内烘焙框体，没有重复套框；
- 开发者工具状态栏未报告项目编译错误。

## 微信开发者工具待复验

静态和自动化门禁不能替代小程序运行环境。提交审核前执行：

2026-07-25 已使用本机微信开发者工具 RC 2.02.2607171、基础库 3.17.0 完成一次模拟器复验：

- 重新编译时发现首页与提问页的自闭合 `<br/>` 不被当前 WXML 编译器接受，已改为块级 `<text>` 换行并重新编译通过；
- 首页正常显示 4/78 进度、主包卡背和历史缩略图；
- 提问页正常显示单牌/三牌和输入区域；
- `packages/deck/pages/collection/index` 普通分包成功加载，五组数量显示为 22、14、14、14、14，已解锁大牌图片与未解锁卡背正常显示；
- 工具“问题”面板未报告项目源代码问题；控制台仍显示开发者工具/Chromium 对已移除 `DOMNodeRemoved` 事件的渲染层兼容提示，项目代码中不存在该事件监听。

CLI 的 IDE“服务端口”处于关闭状态；为避免擅自扩大本机调试暴露面，本轮未修改该安全设置。以下流程级和真机项仍保留为发布前人工门禁。

1. 冷启动首页，确认 Logo、卡背、今日牌和最近记录缩略图加载；模拟器已通过，真机待验；
2. 单牌流程能从主包提问页进入分包仪式，翻牌后进入分包结果页；
3. 三牌流程三张不重复，正逆位和逐牌切换正确；
4. 结果页 AI 正常、超时、非法结构和不安全输出降级均可显示小阿尔卡那；
5. 图鉴显示五组，数量分别为 22、14、14、14、14；模拟器已通过；
6. 解锁小阿尔卡那后，图鉴详情与鉴赏大图可打开；
7. 历史列表和详情能加载大小阿尔卡那，旧 `major-*` Reading 继续可读；
8. 开发者工具“代码依赖分析/上传包”确认主包与 `deck` 分包均未超过限制；
9. 真机复验低网速首次加载分包、卡面清晰度、翻牌帧率和 180° 逆位。

在以上人工项完成前，v1.1 状态为“工程完成、人工复验中”，不宣称已经提交或通过微信审核。
