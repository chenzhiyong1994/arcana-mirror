# 参与贡献

感谢你愿意帮助心镜拾光变得更可靠、更清楚。这个项目优先接受边界明确、能够独立验证的改进。

## 适合贡献的方向

- 可复现的缺陷与微信机型兼容问题；
- 可访问性、文案可读性和渐进披露体验；
- 领域、安全、AI 输出契约与资源门禁测试；
- 小型性能改进、依赖升级和文档修正；
- 不扩大产品风险边界的功能建议。

社区、付费、真人服务、确定性预测和大规模新增牌阵暂不在当前范围内。涉及产品方向的较大提案，请先创建 Feature Request 说明用户问题、边界与验收方式。

## 本地开发

```powershell
npm install
npm run typecheck
npm test
npm run validate:assets
```

`npm install` 会创建被 Git 忽略的 `project.config.json` 与 `apps/miniprogram/config/cloud.ts`。请使用自己的 AppID 和 CloudBase 环境，不要提交本地配置。

## 提交 Pull Request 前

1. 只修改与提案直接相关的文件，避免顺手进行无关重构或全仓格式化。
2. 为行为变化补充最窄、可执行的回归验证。
3. 运行类型检查、测试和受影响的资产门禁。
4. 在 PR 中说明动机、实际改动、验证结果和仍需人工检查的边界。
5. 如果改变 API、环境配置、数据结构或用户工作流，同步更新现有权威文档。

卡牌资产替换必须保留共享正面框、牌阶/标题叠加、正逆位旋转和分包约束，并重新通过对应逐牌语义、数量与一致性门禁。

## 绝对不要提交

- AppSecret、API Key、访问令牌、控制台凭据或私钥；
- 真实小程序 AppID、CloudBase 环境 ID 或个人本机路径；
- 真实用户问题、聊天记录、模型原始响应或可识别个人信息；
- 未经授权的参考图、字体、商业牌组或其他受限素材；
- `project.config.json`、`project.private.config.json`、`apps/miniprogram/config/cloud.ts`、日志、临时包和构建缓存。

发现安全问题时不要提交公开 Issue，请按 [SECURITY.md](SECURITY.md) 私密报告。

## 交流方式

请围绕事实、复现和权衡讨论，不对贡献者作人身评价。维护者可能要求缩小范围、补充验证，或因产品安全边界拒绝某项功能；这不代表否定问题本身。
