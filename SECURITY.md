# 安全策略

## 支持范围

安全修复以 `main` 分支当前版本为准。历史快照和个人 Fork 不在维护范围内。

## 私密报告漏洞

请使用 GitHub 仓库 Security 页面中的 **Report a vulnerability** 私密报告入口。不要在公开 Issue、Discussion 或 Pull Request 中披露可利用细节、真实凭据、用户问题或个人信息。

报告中请尽量包含：

- 受影响的文件、页面、云函数或数据流；
- 最小复现步骤与实际影响；
- 是否涉及凭据、个人信息、AI 提示注入、越权调用或用量滥用；
- 你已经采取的临时缓解措施；
- 在不暴露敏感信息前提下可提供的日志或截图。

维护者会先确认收到报告，再评估严重程度、复现条件与修复方式。在修复发布前，请不要公开漏洞细节。

## 安全边界

- 小程序客户端被视为不可信环境，不能保存 AppSecret、模型密钥或控制台凭据。
- CloudBase 环境 ID 与 AppID 虽不是密钥，开源仓库仍使用本地忽略配置，避免复用维护者运行环境。
- 用户问题只发送给已配置的 CloudBase AI Provider，不进入本地仓库；本地历史只保存在当前设备。
- 客户端直调 AI 不具备强服务端限流。面向更大流量发布前，应迁入具备身份、配额与滥用控制的服务端边界。

本项目是个人非商业作品，目前不设漏洞赏金计划。
